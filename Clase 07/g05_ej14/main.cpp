#include <iostream>

using namespace std;

/*
    14) Dada una lista de números compuesta por grupos, cada grupo separado del
    siguiente por un cero, ingresando un número cero cuando no hay más grupos.

    a) Para cada uno de los grupos el máximo de los números negativos y el
        mínimo de los números positivos. Se informan 2 resultados por cada grupo.
    b) Para cada uno de los grupos el porcentaje de números pares y números
        impares. Se informan 2 resultados por cada grupo.


    Grupo 1: 2, 5, 9, -8, 15, 0
    Grupo 2: 1, 3, -10, -20, 5, 2, 7, 0
    Grupo 3: 5, 4, 1, 0.. 0
*/

int main()
{
    int n;

    cout << "Ingrese un numero";
    cin >> n;

    while(n!=0){

        //a) Resetear los maximos, minimos y las banderas.
        //b) Resetear contadores pares e impares

        while(n!=0){

            // PROCESOS
                // a) BUSCAMOS MAXIMO Y MINIMO
                // b) Contar Pares, contar Impares

            cout << "Ingrese un numero";
            cin >> n;
        }

    //PROCESOS
        // a) Mostramos Maximo y Minimo
        // b) Sacar Porcentaje y mostrar

        cout << "Ingrese un numero";
        cin >> n;
    }

    // PROCESOS

    return 0;
}
